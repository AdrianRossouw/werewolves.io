.card
  .pic(class="color-#{number}")
    - if (me)
      img(src="/assets/player_#{role}_default.svg")
    - else
      img(src="/assets/player_villager_default.svg")
  .info
    h3.name= name
    .occupation= occupation
